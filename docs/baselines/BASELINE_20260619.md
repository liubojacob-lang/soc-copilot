# SOC Copilot Baseline Report — 2026-06-19

Generated at the start of Phase 1 optimization work. These metrics represent the current state before performance, architecture, and quality improvements.

## Test Coverage

### Backend

- **Command**: `cd backend && pytest --cov=.`
- **Result**: 290 passed, 9 failed, 3 skipped
- **Coverage**: **40.87%**
- **Notes**: Failures are concentrated in `tests/test_auth.py` (401 vs expected 2xx). Redis-dependent tests are skipped because Redis is not running locally.

### Frontend

- **Command**: `cd frontend && npm run test:coverage`
- **Result**: 99 passed, 10 test files
- **Coverage**: **0%** (vitest coverage is not configured to instrument application source files; only test files run)
- **Notes**: Coverage config needs to be added in Phase 4.

## Performance

### Backend Startup Time

- **Command**: `uvicorn backend.main:app --host 127.0.0.1 --port 8000`
- **Result**: **1.67s** to "Application startup complete"

### Frontend Production Build

- **Command**: `cd frontend && npm run build`
- **Result**: **Build fails**
- **Error summary**: `Export named 'X' not found in module` errors in `frontend/lib/api/index.ts`, imported by multiple routes and components.
- **First-screen JS size**: N/A (build cannot complete)
- **Lighthouse Performance**: N/A (build cannot complete)

## Code Quality

- **ESLint warnings**: to be measured after CI gate is enabled
- **TypeScript errors**: build fails with module-resolution / missing-export errors
- **mypy / ruff backend**: to be measured after CI gate is enabled

## Repository State

- Root `Lib/site-packages/` embedded virtual environment: removed
- `ai-assistant-release*` release artifacts: moved out of repo root
- `docs/week1/2/3` historical reports: moved to `docs/archive/`
- Frontend `.mjs` debug scripts: moved to `frontend/scripts/debug/`
- Version numbers: synchronized to `0.9.0`

## Next Baseline Targets

- Backend test coverage: ≥ 60%
- Frontend test coverage: ≥ 40% (after instrumenting source files)
- Frontend build: green
- First-screen JS bundle: < 500KB gzip
- LCP: < 2.5s
- Lighthouse Performance: > 75
- Backend startup time: < 2s (already met)
