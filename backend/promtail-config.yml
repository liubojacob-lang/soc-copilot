server:
  http_listen_port: 9080

clients:
  - url: http://loki:3100/loki/api/v1/push

positions:
  filename: /tmp/positions.yaml

scrape_configs:
  - job_name: soc-copilot-logs
    static_configs:
      - targets:
          - localhost
        labels:
          job: soc-copilot
          __path__: /var/log/backend/server.log
